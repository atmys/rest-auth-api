describe('User', () => {

    describe('when creating account', () => {
        it('should reject incorrect parameters', () => { });
        it('should reject already used emails', () => { });
        it('should return signed user with all rights', () => {
        });
    });

    describe('when login', () => {
        it('should reject incorrect parameters', () => { });
        it('should reject already used emails', () => { });
        it('should return signed user', () => {
        });
    });

    describe('when changing email', () => {
        it('should reject not logged user', () => { });
        it('should reject incorrect parameters', () => { });
        it('should reject already used emails', () => { });
        it('should return signed user', () => {
        });
    });

    describe('when changing password', () => {
        it('should reject not logged user', () => { });
        it('should reject incorrect parameters', () => { });
        it('should reject already used emails', () => { });
        it('should save and return signed user', () => {
        });
    });
});